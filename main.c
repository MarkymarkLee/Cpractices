#include <stdio.h>
#include <string.h>

int printstr(char c,int k,int l,int n,int in,int used){
    static int count=0;
    if(count==n){
        return 0;
    }

    int 
}

int main() {
    
    int k,l,n;
    char c[2];
    scanf("%s%d%d%d",c,&k,&l,&n);

    printstr(c[0],k,l,n);

    return 0;
}